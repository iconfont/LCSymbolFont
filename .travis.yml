# reference: http://www.objc.io/issue-6/travis-ci.html

language: objective-c
# before_install: cd Example && pod install && cd -
script:
- xctool test -workspace Example/LCSymbolFont.xcworkspace -scheme LCSymbolFont -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO
